name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  release:
    types: [published]

jobs:
  ci:
    uses: ./.github/workflows/library-ci.yml
    with:
      package-scope: "hyzco"
      publish-registry: "https://npm.pkg.github.com"
